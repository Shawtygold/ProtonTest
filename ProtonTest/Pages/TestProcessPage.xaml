<Page x:Class="ProtonTest.Pages.TestProcessPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProtonTest.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestProcessPage">

    <Grid Margin="40,32" MaxWidth="800">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Name="CategoryTitleText" Text="Заголовок теста" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Name="ProgressText" Text="Вопрос 1 / 30" FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Right"/>
        </Grid>

        <ProgressBar Name="TestProgressBar" Grid.Row="1" Height="4" Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource AccentBrush}" BorderThickness="0" Margin="0,0,0,24"/>

        <Border Grid.Row="2" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="24" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Name="QuestionText" Text="Текст вопроса..." FontSize="16" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="22"/>

                <StackPanel Name="OptionsContainer"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Name="NextBtn" Content="Следующий вопрос" Style="{StaticResource CleanButtonStyle}" Click="Next_Click"/>
        </StackPanel>
    </Grid>
</Page>
